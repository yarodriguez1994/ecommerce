version : '3.1'

services:
  mongo:
    image: mongo:4.4.18-rc0-focal
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGODB: ecommerse
      MONGOHOST: mongodb://localhost:27017 
    volumes:
      - ../database:/data/db
